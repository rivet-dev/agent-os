--- a/exec/src/session_turn_wasi.rs
+++ b/exec/src/session_turn_wasi.rs
@@ -147,10 +147,9 @@ async fn session_turn() -> anyhow::Result<()> {
         ),
     ];
-    // This harness supplies its model, approval policy, filesystem, network, and
-    // credential environment explicitly. Loading arbitrary host CLI settings can
-    // make the pinned runtime reject values written by a newer Codex release (for
-    // example service_tier = "default"). Keep CODEX_HOME for auth storage, but
-    // start from this revision's defaults.
-    let mut config = Config::load_default_with_cli_overrides(overrides)
+    // Load the guest's normal Codex configuration so portable Agent Plugins,
+    // model providers, and other client-supported settings work in agentOS.
+    // The explicit harness overrides above still own the session's model,
+    // approval, filesystem, and shell-snapshot behavior.
+    let mut config = Config::load_with_cli_overrides(overrides)
         .await
         .context("load config")?;
