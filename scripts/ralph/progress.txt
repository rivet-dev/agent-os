# Ralph Progress Log
Started: Mon Apr  6 01:03:18 PM PDT 2026
---

## Codebase Patterns
- service.rs type definitions use `pub(crate)` fields when extracted to state.rs so impl blocks in service.rs can still access them
- service.rs re-exports public types from state.rs via `pub use crate::state::{...}` so lib.rs re-exports continue to work
- Protocol-level deserialized request types (with `#[derive(Deserialize)]`) belong in protocol.rs
- Internal state types without Deserialize belong in state.rs
- Pre-existing flaky tests in security_hardening.rs fail when run in parallel but pass individually (Node import cache race conditions)
- `host_dir` mounts must handle `lstat("/")` because kernel filesystem accounting probes mounted roots before write-limit checks
- Bridge-backed host filesystem adapters must translate missing `stat`/`lstat` results to `ENOENT`; generic `EIO` breaks create-on-write flows on `js_bridge` mounts
- Rust allows splitting impl blocks across modules — struct definition in state.rs, method impls in service.rs
- NativeSidecar<B> fields are `pub(crate)` so impl blocks in vm.rs (and future domain modules) can access them
- Cross-module method calls require `pub(crate)` visibility on the called method (e.g., vm.rs calling service.rs helper methods)
- Shared free functions (kernel_error, audit_fields, emit_security_audit_event, etc.) are `pub(crate)` in service.rs and imported by domain modules
- MountPluginContext<B> is `pub(crate)` with `pub(crate)` fields so vm.rs reconcile_mounts can construct it
- When extracting methods, check test modules — `use super::*` only imports from the parent module, so removed imports need explicit test-module imports

## 2026-04-06 - US-001
- Extracted shared state types from service.rs into new state.rs module (~450 lines)
- Moved 10 JavaScript sync-RPC request types to protocol.rs (JavascriptChildProcessSpawnRequest, etc.)
- state.rs contains: type aliases, constants, public API types (NativeSidecarConfig, DispatchResult, SidecarError), SharedBridge, ConnectionState, SessionState, VmConfiguration, VmState, VmDnsConfig, DnsResolutionSource, all socket/listener types, ActiveProcess, ActiveExecution, ActiveExecutionEvent, SocketQueryKind, ProcNetEntry, ResolvedChildProcessExecution, network context types
- All fields marked pub(crate) so impl blocks in service.rs can access them
- service.rs now imports types from state.rs and protocol.rs instead of defining them inline
- Files changed: crates/sidecar/src/state.rs (new), crates/sidecar/src/protocol.rs (expanded), crates/sidecar/src/service.rs (type defs removed, imports added), crates/sidecar/src/lib.rs (state module declared)
- **Learnings for future iterations:**
  - The struct definitions in state.rs are currently separate from their impl blocks (which remain in service.rs). Later stories (US-002 through US-005) will move impl blocks alongside types to domain modules.
  - lib.rs re-exports through service.rs which re-exports from state.rs: lib.rs → service.rs `pub use crate::state::*` → state.rs
  - HostFilesystem, ScopedHostFilesystem, MountPluginContext, MemoryMountPlugin, JsBridgeMountPlugin were NOT moved to state.rs — they are bridge/plugin types that will go to bridge.rs (US-003) and plugins/ (US-006)
  - security_hardening tests are flaky in parallel due to Node import cache races — not a regression
---

## 2026-04-06 - US-002
- Extracted VM lifecycle functions from service.rs into new vm.rs module (985 lines)
- Methods moved to vm.rs impl block: create_vm, dispose_vm, bootstrap_root_filesystem, configure_vm, snapshot_root_filesystem, dispose_vm_internal, terminate_vm_processes, wait_for_vm_processes_to_exit
- Free functions moved: bridge_permissions, filesystem_operation_label, reconcile_mounts, resolve_cwd, extract_guest_env, parse_resource_limits, parse_resource_limit, parse_resource_limit_u64, parse_vm_dns_config, parse_vm_dns_nameserver, normalize_dns_hostname, build_root_filesystem, convert_root_lower_descriptor, convert_root_filesystem_entry, root_snapshot_entry, apply_root_filesystem_entry, ensure_parent_directories, discover_command_guest_paths
- Made NativeSidecar<B> fields pub(crate) so vm.rs impl block can access self.* fields
- Made cross-module helper methods pub(crate) (session_scope_for, vm_scope_for, require_owned_session, require_owned_vm, respond, vm_lifecycle_event, vm_ownership, vm_has_active_processes, kill_process_internal, poll_event)
- Made shared free functions pub(crate) in service.rs (kernel_error, plugin_error, vfs_error, root_filesystem_error, normalize_path, encode_guest_filesystem_content, audit_fields, emit_security_audit_event, filesystem_permission_capability, dirname)
- Made MountPluginContext<B> pub(crate) with pub(crate) fields
- service.rs reduced from 13,791 to 12,894 lines
- Files changed: crates/sidecar/src/vm.rs (new), crates/sidecar/src/service.rs (code removed, visibility updated), crates/sidecar/src/lib.rs (vm module declared)
- **Learnings for future iterations:**
  - kill_process_internal is shared between vm.rs (terminate_vm_processes) and service.rs (kill_process RPC handler) — stays in service.rs as pub(crate)
  - extract_guest_env and normalize_dns_hostname are also used by execution code in service.rs (build_javascript_socket_path_context and resolve_dns_with_overrides) — made pub(crate) in vm.rs
  - Root filesystem types (decode/encode snapshot, KernelFilesystemEntry, etc.) moved entirely to vm.rs since no other service.rs code uses them
  - Test module `use super::*` only brings in parent module scope — when parent stops importing types (KernelVmConfig, MountTable, FsAccessRequest, etc.), test module needs explicit imports
---

## 2026-04-06 17:18:40 PDT - US-003
- Extracted root filesystem/bootstrap helpers into `crates/sidecar/src/bootstrap.rs` and host bridge/mount plumbing into `crates/sidecar/src/bridge.rs`, with `service.rs` delegating to the new modules
- Fixed follow-on sidecar regressions uncovered by package tests: Python runtime launch hardening, HTTP/HTTPS guest transport agent wiring, JS child-process host cwd mapping, `host_dir` root `lstat`, and js-bridge missing-path errno mapping
- Files changed: `crates/sidecar/src/bootstrap.rs`, `crates/sidecar/src/bridge.rs`, `crates/sidecar/src/lib.rs`, `crates/sidecar/src/service.rs`, `crates/sidecar/src/state.rs`, `crates/sidecar/src/vm.rs`, `crates/sidecar/src/host_dir_plugin.rs`, `crates/sidecar/tests/python.rs`, `crates/execution/src/node_import_cache.rs`, `crates/execution/src/python.rs`, `crates/execution/assets/runners/python-runner.mjs`, `crates/CLAUDE.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - `host_dir` mount roots participate in kernel resource accounting, so root-level metadata ops like `lstat("/")` have to succeed for mounted filesystems
  - `js_bridge`/bridge-backed filesystems need `stat`/`lstat` misses to surface as `ENOENT`, otherwise kernel pre-write limit checks reject new files before the bridge write runs
  - `security_hardening.rs` remains parallel-sensitive; use `cargo test -p agent-os-sidecar -- --test-threads=1` when validating the sidecar package end to end
---
