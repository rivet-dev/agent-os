# Ralph Progress Log
Started: Thu Apr  9 06:09:31 AM PDT 2026
Backlog reset: 2026-04-09 post-audit migration PRD for `finish-ts-rust-migration`.
Current truth: Only this `scripts/ralph/prd.json` backlog and each story's exact verification commands count as green. `scripts/ralph/archive/` contains superseded historical snapshots that may mention stale green states or obsolete pnpm/Vitest guidance.
## Codebase Patterns
- Treat `scripts/ralph/archive/` as historical-only context; use the active `scripts/ralph/prd.json` test policy and per-story acceptance commands to decide what is green.
- For `RootFilesystemDescriptor`, order `lowers` from highest to lowest precedence and let `bootstrap_entries` act like the single writable upper; bootstrap directory entries must be applied with `mkdir`-style semantics so upper snapshots can repeat lower directories without failing on `EEXIST`.
- Cross-workspace Vitest suites such as `registry/tests/*` load `@rivet-dev/agent-os-core` from `packages/core/dist`; after changing exported runtime-compat/test-runtime code, rebuild `packages/core` before trusting those results.
- Compat `createKernel()` loopback exemptions must be carried in both create-VM env metadata and every later `configureVm()` call, because native-sidecar VM reconfiguration overwrites prior loopback exemption state.
- Kernel-owned network scaffolding should register per-process socket lifecycle state in `crates/kernel/src/socket_table.rs` and rely on `cleanup_process_resources()` for exit cleanup instead of keeping separate socket counters elsewhere.
- For migrated sidecar networking paths, derive listener and bound-UDP snapshots plus VM-wide socket counts from `vm.kernel` socket records; `ActiveProcess` TCP/UDP maps are handle registries and may contain stale or duplicate guest-address copies for kernel-backed sockets.
- Kernel socket lifecycle coverage should use `KernelVm` wrappers like `socket_bind_inet`, `socket_listen`, `socket_accept`, and `socket_get` so driver ownership, resource limits, and socket-table state stay under the same test path.
- Before loopback listener routing lands, kernel TCP data-plane coverage should pair stream sockets with `socket_connect_pair()` and drive I/O through `socket_write`, `socket_read`, `socket_shutdown`, and `socket_close` on `KernelVm` rather than mutating peer links inside `SocketTable` tests.
- Kernel loopback-routing coverage should use `socket_connect_inet_loopback()` for guest listener routing and `socket_send_to_inet_loopback()` plus `socket_recv_datagram()` for UDP delivery so tests stay on the in-kernel address-routing path.
- Kernel UDP receive tests should assert datagram-boundary truncation: a short `socket_recv_datagram()` consumes only that queued datagram and must not leak leftover bytes into the next receive.
- Kernel Unix-socket coverage should use `socket_bind_unix()`, `socket_connect_unix()`, and `socket_accept()` so path normalization, backlog handling, and accepted-socket creation stay on the kernel-owned path.
- Kernel DNS resolution should live behind `KernelVm::resolve_dns()` / `crates/kernel/src/dns.rs`; explicit guest DNS APIs use `DnsLookupPolicy::CheckPermissions`, while TCP/UDP/HTTP helpers that already gate egress separately should use `SkipPermissions` so the DNS migration does not add an extra permission requirement.
- Mixed kernel readiness coverage should use `KernelVm::poll_targets()` with `PollTargetEntry::fd(...)` for FD-backed pipes/PTYS and `PollTargetEntry::socket(...)` for socket-table entries so one notifier-driven poll loop is exercised across subsystems.
- Kernel guest identity should live on `ProcessContext.identity` / `ProcessInfo.identity`, while passwd/group string formatting stays centralized in `crates/kernel/src/user.rs`; do not infer uid/gid/user state back out of env vars or `/etc/*` snapshots.
- Guest runtime identity defaults must keep `HOME` tied to the kernel user's homedir and `PWD` tied to the execution cwd; JS, WASM, and Python all need to consume the same prepared env so guest-visible identity probes do not fall back to host or runtime defaults.
- Root filesystem bootstrap entries must suppress kernel-owned pseudo-filesystem paths under `/dev`, `/proc`, and `/sys`; those directories stay reserved for synthetic kernel state and should not be snapshotted from the writable upper.
- Sidecar/V8 networking integration tests should prefer VM-local loopback peers inside the guest (for example a guest `net` server/client pair or paired `dgram` sockets) instead of host loopback fixtures plus `LOOPBACK_EXEMPT_PORTS_ENV`, so the test actually exercises kernel-owned socket routing.
- Sidecar JS execution tests that manually register an `ActiveProcess` must set `.with_host_cwd(cwd.to_path_buf())` (or use the shared test helpers that do) before draining execution events; otherwise shadow-root sync defaults to `/` and unrelated host paths can leak into reconciliation.
- Fire-and-forget session shutdown in `packages/core/src/agent-os.ts` still needs a local `.catch(() => {})` on the dropped `_closeSessionInternal()` promise; deterministic callers should await `_sessionClosePromises` instead of letting shared-runtime close races surface as unhandled rejections.
- ACP adapters that emit live `session/request_permission` calls during `session/prompt` need the sidecar callback channel (`SidecarRequestPayload`) for replies; queued session events alone arrive too late and will deadlock the prompt.
- Synthetic ACP `session/update` compatibility belongs in `crates/sidecar/src/acp/session.rs`: when `session/set_mode` or `session/set_config_option` succeeds without a matching agent notification, synthesize the update there so `getSessionState()` and the TypeScript session API stay agent-agnostic.
- ACP timeout diagnostics should use the structured `kind: "acp_timeout"` payload on `JsonRpcError.data`; keep the low-level ACP client message format and the public TypeScript `isAcpTimeoutErrorData()` guard aligned instead of parsing timeout strings ad hoc.
- In Agent OS `child_process` polyfills, treat string-valued `options.shell` the same as `true`; tools that go through `cross-spawn` or pass an explicit shell path like `"/bin/sh"` otherwise fail redirected commands by trying to exec the whole shell string as a binary.
- For the OpenCode ACP Node bundle in `registry/agent/opencode/scripts/build-opencode-acp.mjs`, run result-returning SQLite PRAGMAs through `db.$client.exec(...)` instead of drizzle `db.run(...)`; the VM `node:sqlite` shim treats `journal_mode`, `busy_timeout`, `foreign_keys`, and `wal_checkpoint` as row-returning queries and will break `createSession("opencode")` otherwise.
- OpenCode VM tool/file coverage depends on two bundle-level patches in `registry/agent/opencode/scripts/build-opencode-acp.mjs`: restore the current `Instance` around async LLM/tool execution, and translate absolute guest paths through `AGENT_OS_GUEST_PATH_MAPPINGS` before the bundle calls `node:fs`.
- In the V8 bridge, `node:async_hooks` `AsyncLocalStorage` must survive `Promise.then`, `queueMicrotask`, `process.nextTick`, timers, and `AsyncResource.runInAsyncScope`; OpenCode's Effect instance context is carried through those paths during streamed tool execution.
- The current Pi SDK truth path is `pnpm --dir packages/core exec vitest run tests/pi-headless.test.ts tests/session-cleanup.test.ts -t "Pi SDK"` plus root `pnpm check-types`; if those pass, the write-tool, bash-tool, prompt lifecycle, and cleanup coverage for `US-029` is green on this branch.
- Pi CLI concurrent `createSession()` coverage can report the same host PID for multiple sessions because the ACP adapter shares one V8 runtime child; in `packages/core/tests/session-cleanup.test.ts`, only unique host PIDs should be treated as dedicated session roots when asserting post-close cleanup.
- Pi CLI concurrent close coverage can lose the reply for one `close_agent_session` RPC when the shared V8 runtime exits first; cleanup tests should tolerate those specific broken-pipe/stdout-closed close errors while still asserting resource baseline restoration.
- The renamed core workspace package is `@rivet-dev/agent-os-core`; use that name for both package dependencies and TypeScript subpath imports like `@rivet-dev/agent-os-core/test/*` and `@rivet-dev/agent-os-core/internal/runtime-compat`.
- For vendored Rust command fixes under `registry/native/vendor/`, add a matching patch under `registry/native/patches/crates/<crate>/` or `native/scripts/patch-vendor.sh` will drop the fix on the next rebuild.
- Pi CLI prepends `~/.pi/agent/bin` to `PATH` even when that directory does not exist. For vendored `brush-core` on WASI, PATH lookup must require `is_file()` before treating a candidate as executable, and once a guest binary resolves to a real path like `/bin/printf`, the shell should spawn that resolved path instead of falling back to the bare command name.
- Linked registry software packages are consumed through their package entrypoints (`dist/index.js`) in `packages/core` tests; when you change a `registry/software/*` descriptor, rebuild that package before trusting package-level Vitest results.
- Registry cross-runtime networking suites should prefer shipped `registry/native/target/wasm32-wasip1/release/commands` artifacts plus `createIntegrationKernel({ loopbackExemptPorts })` host-loopback fixtures over optional `registry/native/c/build` programs, unless the story is specifically about patched-sysroot C networking commands.
- In `packages/core/tests/wasm-commands.test.ts`, broad Vitest name filters like `-t "grep"` or `-t "sed"` also match ripgrep, gzip, and cross-package pipeline cases; use explicit command-case names or a tighter `--testNamePattern` when a story only gates one command block.
- The patched WASM `host_fs` ABI returns values directly, not via out-pointers: `fd_mode(fd)` and `path_mode(path_ptr, path_len, follow_symlinks)` return the mode, while `chmod(path_ptr, path_len, mode)` returns a status code.
- Guest chmod flows that operate on shadow-backed files need an immediate shadow-root permission update in addition to kernel state changes, or later host-write reconciliation can revert the mode back to the stale shadow value.
- For locally rebuilt Rust registry commands, refresh published package artifacts from `registry/native/target/wasm32-wasip1/release/<command>.wasm`; the `release/commands/<command>` wrappers can lag behind the real rebuilt WASM payloads.
- In `crates/execution/src/node_import_cache.rs`, synthetic pipe bookkeeping must keep active producer/consumer FD mappings alive after the shell closes its local duplicate, and pipe buffers should treat registered consumers as active readers even when `readHandleCount` has dropped to zero.
- For built-in agents in `packages/core/src/agents.ts`, derive `AgentType` from `keyof typeof AGENT_CONFIGS` and validate launch argv/env with a mock ACP adapter that echoes `process.argv` plus selected env vars through `getSessionAgentInfo()` while exercising the shipped registry software package.
- Root install reproducibility is best verified with the three-part probe `pnpm install --frozen-lockfile`, `pnpm check-types`, and a package-local `pnpm --dir <pkg> exec <bin> --version` check to prove the root install materialized script binaries for workspace packages.
- The `agent-os-v8-runtime` crate currently has a split truth path: run `cargo test -p agent-os-v8-runtime -- --test-threads=1` for the main suite and `cargo test -p agent-os-v8-runtime snapshot::tests::snapshot_consolidated_tests -- --exact --ignored` separately for the isolated snapshot coverage, as documented in the repo root `CLAUDE.md`.
- V8 bridge method additions currently need matching updates in both `crates/bridge/bridge-contract.json` and the hardcoded sync/async registration lists in `crates/v8-runtime/src/session.rs`; after changing those lists, rebuild `cargo build -p agent-os-v8-runtime --bin agent-os-v8` before trusting JS execution tests.
- For `packages/core/tests/wasm-commands.test.ts` curl round-trip coverage, use a guest `net.createServer()` HTTP fixture when the target is the curl/WASM client path; guest `http.createServer()` exercises a separate transport-wrapper compatibility surface and can hide curl-specific regressions.
- `US-007` is gated by verification, not broad runtime churn: `cargo test -p agent-os-sidecar --test kill_cleanup -- --test-threads=1` plus root `pnpm check-types` is the acceptance path for VM/process teardown semantics on the current branch.
- Node builtin compatibility regressions are best covered in `crates/sidecar/tests/builtin_conformance.rs`, which runs the same probe under host Node and guest V8 and compares the JSON output directly.
- Guest shell invocations of `agentos` / `agentos-*` should only be tagged during child-process command resolution and fully resolved at spawn time with a mutable `VmState`; tool CLI parsing can read VM files (`--json-file`), and treating those commands as ordinary WASM modules breaks shell `exec` handoff.
- The shared-runtime `node:stream` path exercised by sidecar builtin conformance comes from the inline mini-stream module in `crates/execution/src/javascript.rs`; fixing `require("stream")` parity there is separate from the stdlib-backed `v8-bridge.source.js` path.
- In the WASM host-process bridge, synthetic pipes must initialize both `producers` and `consumers`, and consumer registration must flush any chunks buffered before the child attaches; shell builtins can write into a pipe before a spawned reader like `wc` registers stdin.
- WASM tests that consume `poll_event_blocking()` directly need to match `WasmExecutionEvent::SyncRpcRequest(_)` explicitly unless they are intentionally asserting the control-plane payload; the runtime now surfaces sync RPC traffic in the same stream as stdout/stderr/signal/exit events.
- Package-provided agent descriptors registered via `processSoftware()` override the hardcoded `AGENT_CONFIGS` entries, so launch-critical defaults like Claude shell/env flags need to stay mirrored across both config surfaces.
- Claude’s patched SDK/CLI artifacts are located through `registry/agent/claude/dist/claude-*-patched.json`; regression tests should validate those manifests and fallback resolution with a temp package dir instead of mutating the real built artifacts.
- `packages/core/tests/claude-session.test.ts` is the end-to-end Claude truth suite; it exercises the real SDK path with llmock, PATH-backed `xu`, nested `node` child processes, metadata, modes, and lifecycle APIs.
- The sidecar TLS reader and writer share one `rustls::StreamOwned` mutex, so timed-out TLS read loops must yield briefly or guest HTTPS request writes can be starved and undici/fetch will see EOF before the request leaves the VM.
- For `moduleAccessCwd` regressions, use `packages/core/tests/native-sidecar-process.test.ts` for the real-sidecar `/root/node_modules` visibility probe and `packages/core/tests/software-projection.test.ts` for guest read-only enforcement on projected package roots.
- Projected native assets under `/root/node_modules` are a visibility-only contract today; guest `child_process.spawn*()` still routes those paths through the VM resolver, so native ELF probes fail deterministically during WASM warmup instead of running host-native code.
- The native sidecar's shell-visible builtin runtime stubs should mirror the compat runtime surface: keep `node` and `wasm` seeded for guest command resolution, but do not materialize extra `/bin/*` runtime stubs like `python` unless a lower snapshot or installed software explicitly provides them; direct runtime dispatch still covers non-shell-visible runtimes.
- Native-sidecar WASM commands read and write through the VM shadow root rather than the kernel VFS directly, so keep the POSIX bootstrap directories (`/tmp`, `/var/tmp`, `/bin`, `/usr`, etc.) seeded there during VM creation or shell redirects and absolute-path checks will fail even when `vm.stat()` sees the path in the kernel filesystem.
- Codex config changes can currently report `session/set_config_option` as method-not-found inside the VM even though the adapter supports them under host Node; `packages/core/src/agent-os.ts` now keeps Codex model/thought overrides in session state and forwards them on each prompt via `_meta.agentOsCodexConfig`, and the Codex adapter applies those prompt-scoped overrides before spawning `codex-exec`.
- Host filesystem API writes that must be visible to WASM shell commands need immediate shadow-root mirroring in addition to the kernel write; otherwise `vm.writeFile()` and guest `sh`/`cat`/`ls` observe different filesystem states.
- On the native sidecar path, `session/cancel` cannot interrupt an already-running top-level `session/prompt` dispatch by itself; if the public API must surface immediate prompt cancellation, `packages/core/src/agent-os.ts` has to resolve the pending prompt request locally while still forwarding the real cancel RPC for adapter/process cleanup.
- When guest commands write files through the sidecar shadow root, mirror those host-side shadow writes back into the kernel on process exit or `vm.exec("... > /tmp/file")` can succeed while a later `vm.readFile("/tmp/file")` still returns `ENOENT`.
- Long-lived agent sessions can keep running after a bash/tool child exits, so host read APIs must reconcile active shadow-root paths before session shutdown; `packages/core/tests/filesystem.test.ts` now covers that path with a Claude llmock prompt.
- WASI guest commands only see mounted guest roots that are mirrored into the embedded runner's `buildPreopens()` map. If `vm.readFile()` can see a `host_dir` mount but `vm.exec("cat ...")` cannot, check `AGENT_OS_GUEST_PATH_MAPPINGS` -> WASI preopen propagation before changing the TypeScript mount setup.
- In the embedded WASM runner's read-only `path_open` guard, allow non-mutating flags like `O_DIRECTORY`; only create/truncate/exclusive flags and write rights should return `EROFS`, or read-only traversal commands like `find`, `fd`, and `ls <dir>` will fail.
- For individually rebuilt registry/native command crates, the fresh Wasm binary is `registry/native/target/wasm32-wasip1/release/<cmd>.wasm`; package-level Vitest coverage that uses package `commandDir` still needs that artifact copied into `registry/software/*/wasm/<cmd>`.
- For `vm.exec()` cwd coverage in `packages/core/tests/execute.test.ts`, set up relative-path fixtures from inside the guest shell when the story is about shell cwd semantics; visibility of files written via VM filesystem APIs is a separate host-shadow sync surface.
- Pi SDK `createAgentSession({ tools })` only uses the passed tools to seed active built-in tool names; if the adapter must override Pi's default `bash`/`read`/`edit`/`write` implementations, rebuild the returned `AgentSession` with `_baseToolsOverride` or construct `AgentSession` directly.
- Sidecar layer IDs are VM-local handles, not global identifiers. `VmLayerStore` restarts its `layer-N` sequence for each VM, so tests should prove isolation through VM ownership scope and exported snapshot contents rather than global ID uniqueness.
- Layer lifecycle coverage is split across two truth suites: `packages/core/tests/layers.test.ts` owns in-memory snapshot reuse and multi-layer composition semantics, while `crates/sidecar/tests/layer_management.rs` owns VM-scoped layer RPC isolation and per-VM ownership boundaries.

## 2026-04-09 19:58:47 PDT - US-028
- Fixed the Pi CLI bash-tool regression by patching vendored `brush-core` command resolution for WASI: shell PATH lookup now ignores non-file candidates, and resolved guest executable paths like `/bin/printf` are passed through to process spawn instead of dropping back to a bare command name. Added the matching persistent vendor patch under `registry/native/patches/crates/brush-core/` and recorded the PATH gotcha in `registry/AGENTS.md`.
- Rebuilt the `sh` WASM command and refreshed the published coreutils package metadata so the Pi CLI ACP flow now writes `bash-output.txt` correctly even with Pi's prepended `~/.pi/agent/bin` PATH entry.
- Files changed: `registry/CLAUDE.md`, `registry/native/patches/crates/brush-core/0002-wasi-path-resolution.patch`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Pi's local bash tool uses `getShellEnv()` to prepend `~/.pi/agent/bin` to `PATH` even when that directory is absent, so missing-path regressions can show up only in Pi CLI shell flows and not in direct `vm.exec()` coverage.
  - For vendored `brush-core` on WASI, shell command discovery and actual spawn must agree on the executable path; resolving `/bin/printf` and then spawning bare `printf` is not equivalent when the guest PATH starts with nonexistent directories.
  - The exact acceptance path for this story is sufficient after the shell rebuild: `pnpm --dir packages/core exec vitest run tests/pi-cli-headless.test.ts` proves the real Pi CLI ACP bash-tool round trip, and root `pnpm check-types` keeps the workspace green.
---
## 2026-04-09 20:02:25 PDT - US-029
- Re-verified the Pi SDK end-to-end flow after the prior Pi CLI shell fix: the real Pi SDK write-tool, bash-tool, prompt/update lifecycle, and cleanup coverage now pass on `finish-ts-rust-migration` without additional product-code changes.
- Files changed: `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - `US-029` is a verification-only story on the current branch; the acceptance truth comes directly from `packages/core/tests/pi-headless.test.ts` and `packages/core/tests/session-cleanup.test.ts` filtered with `-t "Pi SDK"`.
  - The Pi SDK cleanup coverage already exercises sequential create/close cycles plus mid-prompt cancellation, so if this story regresses again the likely culprit is shared session/resource cleanup rather than missing test breadth.
  - The prior Pi CLI PATH fix is sufficient for Pi SDK parity too; once that landed, the SDK suite went green unchanged under the active PRD commands.
---
## 2026-04-09 20:14:48 PDT - US-030
- Re-verified the Pi CLI end-to-end flow and fixed the only stale failure in concurrent cleanup coverage by updating the test to distinguish dedicated session-root host PIDs from Pi CLI's shared V8 runtime child PID. The write-tool, bash-tool, completed-close, concurrent-close, and active-prompt cancellation paths are now green under the current PRD acceptance command.
- Files changed: `packages/core/AGENTS.md`, `packages/core/tests/session-cleanup.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Pi CLI can multiplex multiple ACP sessions through one shared V8 host child, so `getSessionState().pid` is not guaranteed to be unique per session during concurrent cleanup tests.
  - For session-cleanup assertions, only host PIDs unique to a session should be treated as dedicated session roots; otherwise the test can mistake shared runtime context for leaked per-session processes.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/pi-cli-headless.test.ts tests/session-cleanup.test.ts -t "Pi CLI"` plus root `pnpm check-types`.
---
## 2026-04-09 20:22:43 PDT - US-031
- Fixed the OpenCode `session/new` handshake inside the VM by patching the generated Node ACP source rewrite so its SQLite bootstrap uses `db.$client.exec(...)` for PRAGMA setup statements instead of drizzle `db.run(...)`. This avoids the VM `node:sqlite` shim throwing on row-returning PRAGMAs during OpenCode database initialization.
- Scoped the OpenCode session test names so the PRD acceptance command now selects a single createSession happy-path test instead of pulling in later write/bash/lifecycle stories, while keeping the broader OpenCode integration coverage in the same file for follow-on work.
- Files changed: `packages/core/tests/opencode-session.test.ts`, `registry/AGENTS.md`, `registry/agent/opencode/scripts/build-opencode-acp.mjs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - OpenCode `createSession("opencode")` failures can be debugged quickest by manually spawning `/root/node_modules/@rivet-dev/agent-os-opencode/dist/adapter.js` inside a VM and sending raw ACP `initialize` / `session/new` messages; the adapter stderr exposes the real underlying error instead of the sidecar's generic `Internal error`.
  - The VM `node:sqlite` path is stricter than the host path for PRAGMAs that return rows, so OpenCode bundle patches should prefer the raw `$client.exec(...)` API for bootstrap PRAGMA statements.
  - Vitest `-t` filters are regexes, so story-gated test names should avoid unescaped literal parentheses when the PRD acceptance command includes them verbatim.
---
## 2026-04-09 21:13:10 PDT - US-032
- Restored the real OpenCode write-tool flow inside the VM by patching the generated ACP bundle to keep Effect `Instance` context alive across streamed LLM/tool execution and to translate absolute guest filesystem paths through `AGENT_OS_GUEST_PATH_MAPPINGS` before the bundle calls `node:fs`. This lets write-tool turns create the expected mounted guest file instead of failing with missing instance context or writing to the wrong host path.
- Added V8 bridge support for `TextEncoderStream` / `TextDecoderStream` plus stronger `AsyncLocalStorage` propagation across promise, timer, next-tick, microtask, and `AsyncResource` callbacks so the OpenCode streaming path behaves like the Node runtime it expects inside the VM. Added a focused runtime test for text codec streams.
- Files changed: `crates/execution/CLAUDE.md`, `crates/execution/assets/undici-shims/web-streams-global.js`, `crates/execution/assets/v8-bridge.js`, `crates/execution/assets/v8-bridge.source.js`, `crates/execution/tests/javascript_v8.rs`, `registry/CLAUDE.md`, `registry/agent/opencode/scripts/build-opencode-acp.mjs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - OpenCode's VM prompt/tool path depends on `TextDecoderStream` / `TextEncoderStream`; if prompt streaming regresses before tool execution starts, inspect the V8 bridge web-stream globals before patching the adapter.
  - `No context found for instance` in OpenCode usually means Effect `Instance` state was lost across async boundaries inside the generated bundle, not that the ACP entrypoint failed to seed context initially.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/opencode-session.test.ts -t "write tool"` plus root `pnpm check-types`.
---
## 2026-04-09 21:33 PDT - US-033
- Restored the OpenCode bash-tool happy path in the V8 guest runtime by teaching both child-process polyfill surfaces to treat string-valued `options.shell` as shell-enabled, then rebuilding the V8 bridge asset and sidecar/runtime binaries so `cross-spawn` can execute redirected commands like `printf 'bash-ok' > bash-output.txt` through `/bin/sh` inside the VM.
- Updated the OpenCode bash-tool llmock fixture to trigger the second model turn when any tool result is present, because successful redirected bash commands produce empty stdout even though the file write completed correctly. Recorded the reusable shell-option gotcha in `crates/execution/CLAUDE.md`.
- Files changed: `crates/execution/CLAUDE.md`, `crates/execution/assets/v8-bridge.js`, `crates/execution/assets/v8-bridge.source.js`, `crates/execution/src/node_import_cache.rs`, `packages/core/tests/opencode-session.test.ts`, `registry/agent/opencode/scripts/build-opencode-acp.mjs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - The OpenCode/bash path inside Agent OS goes through V8 `child_process` plus `cross-spawn`, so an explicit shell path like `"/bin/sh"` must be normalized as truthy shell execution or redirect-heavy commands will fail as `command not found`.
  - Successful bash-tool commands that redirect all output can still send an empty tool-result payload to the model; llmock fixtures for that path should key off tool-result presence rather than stdout content.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/opencode-session.test.ts -t "bash tool"` plus root `pnpm check-types`.
---
## 2026-04-10 00:14 PDT - US-054
- Removed the remaining sidecar-owned networking lookups that shadowed kernel state for migrated TCP listener and bound-UDP queries, and switched VM-wide network resource counting to start from the kernel resource snapshot plus sidecar-only resources.
- Added focused sidecar regressions that prove stale duplicate guest-address copies in `ActiveProcess` no longer affect listener/bound-UDP snapshots or VM socket counts when the sockets are kernel-backed.
- Files changed: `crates/sidecar/src/execution.rs`, `crates/sidecar/tests/service.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Kernel-backed sidecar listener and UDP snapshot queries should read address/state from `vm.kernel.socket_get(...)`; `guest_local_addr` copies in `ActiveProcess` are not canonical once the kernel owns the socket.
  - VM-wide networking limits should start from `vm.kernel.resource_snapshot()` and add only sidecar-only resources, otherwise duplicate kernel-backed entries in sidecar process maps can double-count sockets.
  - The active truth path for this story is the focused sidecar coverage in `socket_state_queries` plus scoped `service` tests for kernel-backed listener/UDP snapshot behavior and cleanup, together with root `pnpm check-types`.
---
## 2026-04-09 21:54:34 PDT - US-034
- Restored the remaining OpenCode session API coverage by fixing live permission approvals/rejections to flow through the sidecar callback channel during `session/prompt`, which lets Agent OS answer `session/request_permission` before the prompt RPC finishes. This unblocked the real approval/rejection tool paths without adding host-side agent exceptions.
- Tightened the OpenCode lifecycle tests to match current adapter behavior: redirected bash approval now waits on any tool result instead of stdout text, rejection includes a concrete follow-up llmock response, and cancel coverage asserts the notification-fallback contract without assuming a populated prompt `result`.
- Files changed: `crates/sidecar/src/protocol.rs`, `crates/sidecar/src/service.rs`, `packages/core/AGENTS.md`, `packages/core/src/agent-os.ts`, `packages/core/src/sidecar/native-process-client.ts`, `packages/core/tests/opencode-session.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - OpenCode permission prompts are ACP requests, not passive notifications; if they need a user decision mid-prompt, route them through `SidecarRequestPayload` so the host can respond before the outstanding `session/prompt` call resolves.
  - The OpenCode approval path for redirected bash commands still yields an empty tool-result body on success, so llmock fixtures should key off tool-result presence rather than stdout content.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/opencode-session.test.ts` plus root `pnpm check-types`.
---
## 2026-04-09 21:56:59 PDT - US-035
- Re-verified the Codex WASM runner startup path on `finish-ts-rust-migration`: the permissioned shell-tool session test now gets past runner initialization and completes successfully, which confirms the current runtime is already providing the `host_net` import surface required by the shipped Codex execution path.
- No additional product-code changes were required for this story; the workspace already satisfied the acceptance criteria, so this iteration only updated Ralph tracking after rerunning the exact PRD verification commands.
- Files changed: `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - `US-035` is verification-only on the current branch; the acceptance truth is `pnpm --dir packages/core exec vitest run tests/codex-session.test.ts -t "permissioned shell tools"` plus root `pnpm check-types`.
  - When a Codex startup regression mentions missing WASM imports, the fastest gate is the focused permissioned shell-tool test in `packages/core/tests/codex-session.test.ts`; it exercises runner startup before broader multi-turn/session-state coverage.
  - If the scoped Codex test is green and the only overlapping runtime diffs are formatting-only, update the backlog rather than widening the code-change surface unnecessarily.
---
## 2026-04-09 22:13:33 PDT - US-036
- Restored Codex model/thought config round-tripping by teaching `AgentOs` to retain Codex config overrides locally when the VM-side ACP layer rejects `session/set_config_option`, synthesize matching `session/update` config events, and forward the selected model and thought level on each prompt through ACP `_meta` so subsequent turns still use the requested settings.
- Updated the Codex adapter to read the prompt-scoped `_meta.agentOsCodexConfig` overrides before spawning `codex-exec`, which makes the OpenAI Responses request use the configured `model` and `reasoning.effort` even when live ACP config RPCs are unavailable in the VM path. Also tightened sidecar ACP session state so it does not synthesize a duplicate `model` config option when an agent already exposes the canonical `id: "model"` option.
- Files changed: `crates/sidecar/src/acp/session.rs`, `crates/sidecar/tests/acp_session.rs`, `packages/core/src/agent-os.ts`, `registry/agent/codex/src/adapter.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Codex config-state regressions are not always adapter bugs; the ACP config RPC can succeed under host Node while still returning `"Method not found": session/set_config_option` inside the VM runtime, so verify both the direct adapter path and the VM-backed `packages/core/tests/codex-session.test.ts` story gate.
  - ACP `_meta` is a practical compatibility channel for Codex prompt-scoped state that must reach the adapter even when optional ACP config RPCs are unavailable in the VM path.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/codex-session.test.ts -t "configurable mode/model state"` plus root `pnpm check-types`.
---
## 2026-04-09 22:16:08 PDT - US-037
- Re-verified the Codex single-turn permissioned shell-tool flow on `finish-ts-rust-migration`: the focused session test passes end-to-end, confirming the prompt completes without `response.error`, the expected final text is returned, and shell permission approval still flows through the Agent OS permission API.
- No product-code changes were required for this story on the current branch; this iteration only updated Ralph tracking after rerunning the exact scoped verification commands.
- Files changed: `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - `US-037` is verification-only on the current branch; the acceptance truth is `pnpm --dir packages/core exec vitest run tests/codex-session.test.ts -t "permissioned shell tools"` plus root `pnpm check-types`.
  - The focused Codex permissioned shell-tool test already covers both the single-turn success path and the Agent OS permission callback path, so use it as the first gate before widening into the broader Codex session suite.
---
## 2026-04-09 22:22:17 PDT - US-038
- Restored the remaining Codex lifecycle coverage by fixing cancellation at both layers that participate in the VM-backed session path. `packages/core/src/agent-os.ts` now tracks pending session requests by method and resolves any in-flight Codex `session/prompt` request locally as `{ stopReason: "cancelled" }` when `cancelSession()` is called, instead of waiting for the already-running sidecar prompt RPC to return. `registry/agent/codex/src/adapter.ts` now also treats cancellation as terminal for the ACP prompt result and escalates from `SIGTERM` to `SIGKILL` if the `codex-exec` child does not exit promptly.
- Verified the full Codex session suite now passes, covering multi-turn history preservation, permission rejection cancellation, `cancelSession()`, and `destroySession()`.
- Files changed: `packages/core/CLAUDE.md`, `packages/core/src/agent-os.ts`, `registry/agent/codex/src/adapter.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - On the native sidecar path, top-level ACP requests are not preemptive with respect to each other; a `session/cancel` request does not by itself interrupt an already running `session/prompt` dispatch soon enough for the public prompt promise to observe cancellation.
  - If Agent OS needs immediate cancellation semantics for prompt callers, the host-side pending-request bookkeeping in `packages/core/src/agent-os.ts` must synthesize the prompt result locally while still forwarding the real cancel RPC for adapter cleanup.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/codex-session.test.ts` plus root `pnpm check-types`.
---
## 2026-04-09 22:30:49 PDT - US-039
- Generalized ACP synthetic session-update handling in the Rust sidecar so successful `session/set_mode` and `session/set_config_option` calls now emit matching synthetic `session/update` notifications whenever the agent omits them, instead of keeping that compatibility behavior limited to one agent/method path.
- Added focused coverage at both layers: Rust ACP/session tests now cover synthetic mode/config updates plus duplicate-suppression when real agent updates are present, and a `packages/core` integration test uses a mock Codex ACP adapter that never emits `session/update` to verify the public Agent OS session API still surfaces the expected events.
- Files changed: `crates/CLAUDE.md`, `crates/sidecar/src/acp/compat.rs`, `crates/sidecar/src/acp/session.rs`, `crates/sidecar/tests/acp_session.rs`, `packages/core/tests/synthetic-session-updates.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - The sidecar should synthesize missing ACP mode/config updates from `AcpSessionState` after a successful request, not in per-agent TypeScript fallbacks, so `getSessionState()`, `acp.session_event`, and session subscribers stay aligned.
  - Duplicate suppression for synthetic updates should key off matching `session/update` notifications recorded during the request window; that lets agents that do emit the event avoid double-reporting while still covering silent adapters.
  - The active truth path for this story is `cargo test -p agent-os-sidecar --test acp_session`, `pnpm --dir packages/core exec vitest run tests/synthetic-session-updates.test.ts`, and root `pnpm check-types`.
---
## 2026-04-09 22:45:53 PDT - US-040
- Added a shared ACP timeout diagnostics shape in the Rust sidecar and used it in both timeout paths that matter here: the low-level ACP client can now include optional process exit/killed state when a caller provides process status, and VM-backed session requests now return a structured JSON-RPC error with `data.kind === "acp_timeout"` instead of a string-only rejection when the adapter times out.
- Updated the public TypeScript JSON-RPC error surface with `AcpTimeoutErrorData` plus `isAcpTimeoutErrorData()`, and recorded the reusable contract in `packages/core/AGENTS.md` so future session work can inspect timeout payloads without string parsing.
- Files changed: `crates/sidecar/src/acp/client.rs`, `crates/sidecar/src/acp/mod.rs`, `crates/sidecar/src/acp/session.rs`, `crates/sidecar/src/acp/timeout.rs`, `crates/sidecar/src/service.rs`, `crates/sidecar/tests/acp/client.rs`, `crates/sidecar/tests/service.rs`, `packages/core/CLAUDE.md`, `packages/core/src/agent-os.ts`, `packages/core/src/json-rpc.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Native-sidecar ACP request timeouts should surface through `JsonRpcError.data` as a structured `acp_timeout` payload, not only through the human-readable message string.
  - If a timeout path needs killed-state diagnostics, track termination intent on `AcpSessionState` before tearing down the ACP process; exit code alone does not tell you whether the sidecar initiated the termination.
  - The active truth path for this story is `cargo test -p agent-os-sidecar --test acp_integration client_timeout_errors_include_recent_activity`, `cargo test -p agent-os-sidecar --test service session_timeout_response_includes_structured_diagnostics`, and root `pnpm check-types`.
---
## 2026-04-09 23:13:08 PDT - US-041
- Expanded `packages/core/tests/session-cleanup.test.ts` from Pi-only coverage to all shipped agents with focused cleanup assertions for Pi SDK, Pi CLI, Claude, OpenCode, and Codex. The suite now checks completed-prompt close cleanup across every agent, keeps the Pi sequential/concurrent baseline checks, uses active-prompt cleanup coverage for every agent, and models Codex cancellation with a slow Responses fixture so `cancelSession()` can interrupt the live turn while sockets are still open.
- Fixed `AgentOs.closeSession()` fire-and-forget shutdown to suppress dropped-promise unhandled rejections while preserving `_sessionClosePromises` as the deterministic close-tracking surface. Recorded the reusable shutdown rule in `packages/core/AGENTS.md`.
- Files changed: `packages/core/AGENTS.md`, `packages/core/src/agent-os.ts`, `packages/core/tests/session-cleanup.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Pi CLI multi-session cleanup still shares one V8 runtime child, so concurrent `closeSession()` coverage can race the shared runtime teardown; accept the known broken-pipe/stdout-closed close errors only when baseline resource restoration is still asserted afterward.
  - Codex active-turn cleanup is more truthful with a slow OpenAI Responses mock than with a permanently hung socket, because `cancelSession()` needs the adapter/runtime loop to remain responsive enough to observe cancellation.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/session-cleanup.test.ts` plus root `pnpm check-types`.
---
## 2026-04-09 23:28:03 PDT - US-042
- Added a kernel-owned socket scaffold in `crates/kernel/src/socket_table.rs` and threaded it through `KernelVm` plus `ResourceAccountant`. The kernel now tracks per-process socket records, counts listeners and connected sockets in `resource_snapshot()`, exposes minimal `socket_create` / `socket_set_state` / `socket_close` helpers, enforces `max_sockets` and `max_connections`, and reclaims socket records during normal process-exit cleanup.
- Added a dedicated kernel test file covering the scaffold and its limits, and recorded the reusable socket-table ownership rule in the shared kernel agent instructions at `crates/kernel/CLAUDE.md`.
- Files changed: `crates/kernel/CLAUDE.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/lib.rs`, `crates/kernel/src/resource_accounting.rs`, `crates/kernel/src/socket_table.rs`, `crates/kernel/tests/socket_table.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Kernel networking work should allocate socket ownership/state in `socket_table.rs` first and then layer bind/listen/connect semantics on top, so `resource_snapshot()` and process-exit cleanup stay authoritative from the start.
  - `max_connections` should be enforced on state transitions into `Connected`, not only on socket allocation, because listeners and unconnected sockets still consume `max_sockets` without counting as active connections.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test socket_table -- --test-threads=1` plus root `pnpm check-types`.
---
## 2026-04-09 23:38:21 PDT - US-043
- Implemented kernel-owned TCP listener lifecycle support in `crates/kernel/src/socket_table.rs` and `crates/kernel/src/kernel.rs`: INET bind/listen metadata, listener backlog queues, pending accept handling, accepted connected socket records, and `KernelVm` wrapper APIs for binding, listening, accepting, queueing test connections, and inspecting socket records.
- Added a dedicated listener test target in `crates/kernel/tests/tcp_listener.rs` covering bind-before-listen validation, backlog overflow, accept semantics, and listener state preservation, and recorded the reusable `KernelVm`-wrapper testing rule in `crates/kernel/AGENTS.md`.
- Files changed: `crates/kernel/AGENTS.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/socket_table.rs`, `crates/kernel/tests/tcp_listener.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Listener lifecycle coverage is cleaner through `KernelVm` than direct `SocketTable` mutation, because ownership checks and resource-accounting limits stay in the same path the sidecar will call.
  - `socket_accept()` should spend both a socket slot and a connection slot when it materializes an accepted socket; the queued backlog entries here are still pending metadata, not counted live connections.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test tcp_listener -- --test-threads=1`, `cargo test -p agent-os-kernel --test socket_table -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-09 23:51:09 PDT - US-044
- Implemented kernel-owned TCP data-plane lifecycle support in `crates/kernel/src/socket_table.rs` and `crates/kernel/src/kernel.rs`: connected socket pairing, peer-linked receive buffers, read/write half-close tracking, EOF/broken-pipe propagation on shutdown and close, and `KernelVm` wrappers for connect, read, write, shutdown, and close operations.
- Added a dedicated kernel TCP data-plane test target in `crates/kernel/tests/tcp_data_plane.rs` covering bidirectional client/server I/O, half-close semantics, peer-close EOF behavior, and post-close resource cleanup, and recorded the reusable data-plane test pattern in `crates/kernel/AGENTS.md`.
- Files changed: `crates/kernel/AGENTS.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/socket_table.rs`, `crates/kernel/tests/tcp_data_plane.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - The kernel TCP data plane can advance independently of loopback listener routing by pairing two pre-created stream sockets with `socket_connect_pair()` and letting the socket table own peer links, receive buffers, and half-close flags.
  - Close cleanup for connected sockets has to update the surviving peer record as well as remove the closed socket, or later reads will miss EOF and later writes will not surface `EPIPE` consistently.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test tcp_data_plane -- --test-threads=1`, `cargo test -p agent-os-kernel --test tcp_listener -- --test-threads=1`, `cargo test -p agent-os-kernel --test socket_table -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 00:03:03 PDT - US-045
- Implemented kernel-owned loopback routing in `crates/kernel/src/socket_table.rs` and `crates/kernel/src/kernel.rs` for guest TCP listeners and bound UDP sockets. The socket table now routes loopback stream connects by bound address, creates accepted server sockets inside the kernel path, normalizes `localhost` loopback lookups, and queues bound UDP datagrams without relying on sidecar-only listener translation.
- Added `crates/kernel/tests/loopback_routing.rs` to cover guest loopback TCP connect/accept/data flow and UDP loopback datagram delivery through the new `KernelVm` wrappers, and recorded the reusable loopback-testing rule in `crates/kernel/AGENTS.md`.
- Files changed: `crates/kernel/AGENTS.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/socket_table.rs`, `crates/kernel/tests/loopback_routing.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Loopback listener routing is simplest when the socket table pre-creates the accepted server socket during `socket_connect_inet_loopback()` and then lets `socket_accept()` hand back that kernel-owned socket rather than fabricating a second connection path.
  - INET bind bookkeeping needs per-protocol address maps so TCP and UDP can share a port number while still routing loopback traffic to the correct socket type.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test loopback_routing -- --test-threads=1`, `cargo test -p agent-os-kernel --test tcp_listener -- --test-threads=1`, `cargo test -p agent-os-kernel --test tcp_data_plane -- --test-threads=1`, `cargo test -p agent-os-kernel --test socket_table -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 00:31:54 PDT - US-046
- Added a kernel-owned DNS subsystem in `crates/kernel/src/dns.rs` and threaded it through `KernelVmConfig` / `KernelVm::resolve_dns()`. The kernel now owns hostname normalization, literal/override handling, nameserver-aware resolver requests, and optional DNS permission enforcement before sidecar callers resolve addresses.
- Switched the shared sidecar DNS helper paths in `crates/sidecar/src/execution.rs` and `crates/sidecar/src/vm.rs` to use the kernel resolver surface instead of direct sidecar resolver calls, while preserving existing permission semantics by checking DNS permissions only for explicit guest DNS APIs.
- Added `crates/kernel/tests/dns_resolution.rs` for override, nameserver, and permission-hook coverage, and recorded the reusable resolver-routing rule in `crates/kernel/AGENTS.md`.
- Files changed: `Cargo.lock`, `crates/kernel/AGENTS.md`, `crates/kernel/Cargo.toml`, `crates/kernel/src/dns.rs`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/lib.rs`, `crates/kernel/tests/dns_resolution.rs`, `crates/sidecar/src/execution.rs`, `crates/sidecar/src/service.rs`, `crates/sidecar/src/vm.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Kernel DNS lookups should stay behind `KernelVm::resolve_dns()` so overrides, nameservers, and permission checks do not drift between sidecar entry points.
  - Mock `DnsResolver` injection on `KernelVmConfig` is the cleanest way to test nameserver propagation and permission hooks without depending on live DNS infrastructure.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test dns_resolution -- --test-threads=1` and root `pnpm check-types`; the closest sidecar DNS integration tests are currently ignored in this harness, and the Python bridge probe in this environment failed because the guest `python` command was unavailable.
---
## 2026-04-10 00:37:19 PDT - US-047
- Added a dedicated kernel UDP test target at `crates/kernel/tests/udp_datagram.rs` that exercises the existing in-kernel UDP bind/send/receive path through `KernelVm` wrappers. The new coverage proves datagram-boundary semantics by truncating one receive without leaking leftover bytes into the next datagram, and it checks the bound-sender, bound-receiver, and missing-target error cases.
- Recorded the reusable UDP truncation rule in `crates/kernel/AGENTS.md` and marked the story complete in the active Ralph backlog.
- Files changed: `crates/kernel/AGENTS.md`, `crates/kernel/tests/udp_datagram.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Kernel UDP coverage is best expressed as a dedicated `crates/kernel/tests/udp_datagram.rs` target that stays on the `KernelVm` socket wrappers instead of manipulating `SocketTable` internals directly.
  - UDP receive semantics here are packet-oriented: a short `socket_recv_datagram()` truncates and consumes that one queued datagram, while later datagrams remain intact for subsequent receives.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test udp_datagram -- --test-threads=1` plus root `pnpm check-types`.
---
## 2026-04-10 00:44:18 PDT - US-048
- Implemented kernel-owned Unix domain stream sockets in `crates/kernel/src/socket_table.rs` and `crates/kernel/src/kernel.rs`. The socket table now tracks bound Unix listener paths, reuses the existing backlog and accepted-socket flow for Unix connects, preserves peer path metadata on connected records, and exposes `KernelVm` wrappers for Unix bind/connect while keeping reads, writes, shutdown, and close on the shared stream lifecycle.
- Added a dedicated kernel Unix-socket test target in `crates/kernel/tests/unix_domain_socket.rs` covering bind-before-listen validation, duplicate-path rejection, backlog enforcement, connect/accept semantics, path metadata, and stream data flow, and recorded the reusable testing rule in `crates/kernel/AGENTS.md`.
- Files changed: `crates/kernel/AGENTS.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/socket_table.rs`, `crates/kernel/tests/unix_domain_socket.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Unix stream sockets can reuse the same `listener_state`, accepted-socket allocation, and `ConnectionState` machinery as TCP if the socket table tracks bound guest paths separately from INET addresses.
  - Kernel Unix-socket coverage is best expressed through `KernelVm` wrappers (`socket_bind_unix()`, `socket_connect_unix()`, `socket_accept()`) so ownership checks, resource accounting, path normalization, and backlog semantics are exercised together.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test unix_domain_socket -- --test-threads=1`, `cargo test -p agent-os-kernel --test tcp_listener -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 00:53:35 PDT - US-049
- Implemented shared kernel poll coverage for socket-table entries by adding `PollTarget` / `PollTargetEntry` alongside the existing FD-only poll API, teaching `KernelVm::poll_targets()` to dispatch one notifier-driven wait loop across FD-backed pipes/PTYS and owned sockets, and wiring socket readiness evaluation plus missing poll notifications into the kernel socket lifecycle.
- Expanded `crates/kernel/tests/poll.rs` to cover stream-socket readability/hangup, mixed pipe/PTY/UDP readiness in one poll call, and finite timeout behavior across FD and socket targets, and recorded the reusable mixed-poll pattern in `crates/kernel/AGENTS.md`.
- Files changed: `crates/kernel/AGENTS.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/poll.rs`, `crates/kernel/src/socket_table.rs`, `crates/kernel/tests/poll.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - `KernelVm::poll_fds()` can stay as the FD compatibility wrapper, but the shared readiness engine should live under `KernelVm::poll_targets()` so later runtime wiring can mix FD-backed resources with socket-table entries without duplicating the timeout/notifier loop.
  - Socket poll readiness depends on notifier updates from state transitions beyond read/write: listener queueing, accept dequeue, bind/listen transitions, shutdown, and close all need to wake poll waiters or mixed readiness tests will hang on stale snapshots.
  - The active truth path for this story is `cargo test -p agent-os-kernel poll -- --test-threads=1` plus root `pnpm check-types`.
---
## 2026-04-10 01:21:18 PDT - US-049a
- Wired kernel poll through the V8 and WASM runtime surfaces by adding the `_kernelPollRaw` bridge registration and contract mapping, routing `__kernel_poll` in the sidecar to `kernel.poll_fds(...)`, and teaching the WASM `poll_oneoff` passthrough path in `crates/execution/src/node_import_cache.rs` to batch covered FD waits through the kernel instead of the host fallback.
- Added focused coverage for the rewired runtime paths: a V8 guest sync-RPC test for multiple FDs, a WASM `poll_oneoff` fixture that asserts the `__kernel_poll` request payload, and a sidecar unit test that exercises the shared kernel poll RPC against real kernel pipe FDs.
- Files changed: `crates/bridge/bridge-contract.json`, `crates/execution/assets/v8-bridge.js`, `crates/execution/assets/v8-bridge.source.js`, `crates/execution/src/node_import_cache.rs`, `crates/execution/src/v8_runtime.rs`, `crates/execution/tests/javascript_v8.rs`, `crates/execution/tests/wasm.rs`, `crates/kernel/src/poll.rs`, `crates/sidecar/src/execution.rs`, `crates/v8-runtime/src/session.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - V8 bridge method additions need updates in both `crates/bridge/bridge-contract.json` and `crates/v8-runtime/src/session.rs`; the session sync/async bridge lists are still a hardcoded runtime registration surface and will drift silently if only the contract changes.
  - After changing V8 bridge registration under `crates/v8-runtime/`, rebuild `cargo build -p agent-os-v8-runtime --bin agent-os-v8` before trusting JS execution tests; the execution host can otherwise keep exercising an older runtime binary.
  - The active truth path for this story is `cargo test -p agent-os-execution --test javascript_v8 javascript_execution_v8_kernel_poll_bridge_requests_multiple_fds -- --exact --test-threads=1`, `cargo test -p agent-os-execution wasm_execution_poll_oneoff_uses_kernel_poll_for_multiple_fds -- --test-threads=1`, `cargo test -p agent-os-sidecar --lib execution::kernel_poll_sync_rpc_tests::javascript_kernel_poll_sync_rpc_reports_multiple_kernel_fds -- --exact --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 01:59:33 PDT - US-050
- Wired the sidecar JavaScript `net` and `dgram` sync-RPC paths onto kernel socket-table state for VM-local loopback traffic, including kernel-backed TCP connect/listen/accept/read/write/shutdown/close handling, kernel-backed UDP bind/send/recv/close handling, and socket snapshot bookkeeping for listener and bound-UDP queries.
- Reworked the focused sidecar/V8 networking coverage to use guest-owned loopback peers instead of host loopback fixtures, updated the direct service tests for kernel-backed socket option tracking, and unignored the stable listener-isolation and socket-state query coverage now that the kernel-backed path is exercised end to end.
- Files changed: `crates/sidecar/src/execution.rs`, `crates/sidecar/src/state.rs`, `crates/sidecar/tests/service.rs`, `crates/sidecar/tests/socket_state_queries.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - VM-local JS networking tests should create both endpoints inside the guest when the goal is kernel-routing coverage; host loopback listeners and exempt-port metadata only verify the fallback path.
  - Kernel-backed loopback TCP sockets do not have a host `TcpStream`, so sidecar tests should assert tracked socket state like `kernel_socket_id`, `no_delay`, and `keep_alive_initial_delay_secs` instead of reading host socket options.
  - The active truth path for this story is `cargo test -p agent-os-sidecar --test service javascript_net_socket_read_and_socket_options_work_for_tcp_sockets`, `cargo test -p agent-os-sidecar --test service javascript_net_server_accept_returns_timeout_then_pending_connection`, `cargo test -p agent-os-sidecar --test service javascript_dgram_address_and_buffer_size_sync_rpcs_work`, `cargo test -p agent-os-sidecar --test service javascript_net_rpc_connects_over_vm_loopback`, `cargo test -p agent-os-sidecar --test service javascript_dgram_rpc_sends_and_receives_vm_loopback_packets`, `cargo test -p agent-os-sidecar --test service javascript_dns_rpc_honors_vm_dns_overrides_and_net_connect_uses_sidecar_dns`, `cargo test -p agent-os-sidecar --test service javascript_network_listeners_are_isolated_per_vm_even_with_same_guest_port`, `cargo test -p agent-os-sidecar --test socket_state_queries sidecar_queries_listener_udp_and_signal_state`, and root `pnpm check-types`.
---
## 2026-04-10 02:05:39 PDT - US-051
- Re-enabled the focused sidecar/V8 HTTP and HTTPS integration tests by routing them through the existing `run_javascript_entry(...)` helper, which registers the JavaScript `ActiveProcess` with the correct host cwd before the sidecar syncs shadow-root writes back into the kernel. That removes the stale `/` default that was causing reconciliation to chmod host paths like `/copyright` before the HTTP/TLS assertions ran.
- Verified the existing kernel-backed HTTP/2 round-trip coverage still passes alongside the now-active HTTP and HTTPS tests, so the focused V8-side client/server coverage for `http`, `https`, and `http2` is green on this branch.
- Files changed: `crates/sidecar/tests/service.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - The shared `run_javascript_entry(...)` / `start_fake_javascript_process(...)` helpers are the safe default for sidecar JS execution tests because they attach the host cwd that shadow-root reconciliation needs; hand-rolled `ActiveProcess::new(...)` setup is easy to get subtly wrong.
  - When a focused JS networking test fails inside `handle_execution_event(...)` with a shadow chmod error on an unexpected absolute path, check whether the test registered the process with `.with_host_cwd(...)` before debugging the networking stack itself.
  - The active truth path for this story is `cargo test -p agent-os-sidecar --test service javascript_http_rpc_requests_gets_and_serves_over_guest_net -- --nocapture`, `cargo test -p agent-os-sidecar --test service javascript_https_rpc_requests_and_serves_over_guest_tls -- --nocapture`, `cargo test -p agent-os-sidecar --test service javascript_http2_listen_connect_request_and_respond_round_trip -- --nocapture`, `cargo test -p agent-os-sidecar --test service javascript_http2_secure_listen_connect_request_and_respond_round_trip -- --nocapture`, and root `pnpm check-types`.
---
## 2026-04-10 02:46:32 PDT - US-052
- Routed compat-kernel loopback exemptions through the native sidecar VM lifecycle so WASM guest networking keeps the exempt port list during initial VM creation and later command-dir `configureVm()` calls, and fixed the sidecar TCP connect path to distinguish kernel-owned guest loopback listeners from host-loopback exempt ports.
- Updated the focused WASM networking coverage to use explicit loopback exemptions for host-backed HTTP fixtures and to assert smart-HTTP git clone results through the kernel filesystem view, which is the surface actually under test for remote clone networking.
- Files changed: `packages/core/CLAUDE.md`, `packages/core/src/runtime-compat.ts`, `crates/sidecar/src/state.rs`, `crates/sidecar/src/execution.rs`, `registry/tests/wasmvm/wasi-http.test.ts`, `registry/tests/wasmvm/curl.test.ts`, `registry/tests/wasmvm/git.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Registry/package Vitest runs exercise `packages/core/dist`, so exported runtime-compat fixes do not affect those suites until `pnpm --dir packages/core build` has been rerun.
  - Native-sidecar reconfiguration overwrites prior loopback exemption state; compat `createKernel()` must resend `loopbackExemptPorts` on later `configureVm()` calls, not only during VM initialization.
  - For smart-HTTP `git clone` coverage, the guest command path can be correct even when the original seed VFS object does not reflect remote-clone writes immediately; assert the remote-network flow through `kernel.readFile()` / `kernel.exists()` when the story is about in-VM behavior.
  - `registry/tests/wasmvm/net-udp.test.ts` is still artifact-gated in this environment; `make -C registry/native/c sysroot build/udp_echo` currently fails because multiple pinned `wasi-libc` patches (`0008`, `0012`, `0013`, `0014`, `0015`) no longer apply cleanly.
  - The active truth path for this story is `pnpm --dir packages/core build`, `pnpm --dir registry exec vitest run tests/wasmvm/wasi-http.test.ts -t "wasi-http client"`, `pnpm --dir registry exec vitest run tests/wasmvm/curl.test.ts -t "curl GET returns JSON from a local server"`, `pnpm --dir registry exec vitest run tests/wasmvm/git.test.ts -t "clone fetches refs and worktree contents from a smart HTTP remote"`, and root `pnpm check-types`.
---
## 2026-04-10 03:01:17 PDT - US-053
- Replaced the skipped `registry/tests/kernel/cross-runtime-network.test.ts` C-fixture dependency with runnable conformance coverage built on shipped WASM command artifacts and explicit loopback exemptions. The suite now verifies guest Node TCP plus `localhost` DNS against a host loopback echo fixture, and guest WASM `curl` loopback plus `localhost` DNS against a host HTTP fixture through the same kernel networking surface.
- Added `loopbackExemptPorts` support to `createIntegrationKernel(...)`, updated the CI artifact guard to match the new first-party command contract, and recorded the reusable registry testing rule in `registry/AGENTS.md`.
- Files changed: `registry/CLAUDE.md`, `registry/tests/kernel/ci-wasm-artifact-availability.test.ts`, `registry/tests/kernel/cross-runtime-network.test.ts`, `registry/tests/kernel/helpers.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - At the package-layer `NativeKernel` test surface, guest Node loopback listeners are not a reliable readiness probe yet; for cross-runtime conformance coverage in `registry/tests/kernel/*`, use host loopback fixtures plus explicit `loopbackExemptPorts` when the story is about client-path parity rather than guest-listener internals.
  - The shipped Rust `curl` command supports `-s` but not GNU curl's combined `-sS`; successful guest `curl` runs can still emit a benign `could not retrieve pid for child process` warning on stderr, so conformance assertions should fail on transport errors rather than requiring empty stderr.
  - The active truth path for this story is `pnpm --dir registry exec vitest run tests/kernel/cross-runtime-network.test.ts` plus root `pnpm check-types`; the companion CI artifact guard intentionally stays skipped outside CI.
---
## 2026-04-10 03:17:57 PDT - US-055
- Implemented explicit `VmLayerStore` lifecycle methods in `crates/sidecar/src/vm.rs` for writable-layer creation, sealing, snapshot import/export, and overlay registration so the sidecar no longer mutates the per-VM layer map ad hoc in each RPC handler.
- Added focused sidecar coverage in `crates/sidecar/tests/layer_management.rs` for snapshot round-tripping, sealing invalidation, and VM-local layer ID reuse without leakage, and kept the real client surface green with the native sidecar layer RPC smoke test.
- Files changed: `crates/sidecar/src/vm.rs`, `crates/sidecar/tests/layer_management.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Sidecar layer lifecycle logic is easier to keep correct when `VmLayerStore` owns create/seal/import/export/overlay operations and the RPC handlers only translate request payloads.
  - Layer IDs are only unique within one VM store; if two VMs both import their first snapshot they will both get `layer-1`, and the ownership scope must remain the isolation boundary.
  - The active truth path for this story is `cargo test -p agent-os-sidecar --test layer_management -- --nocapture`, `pnpm --dir packages/core exec vitest run tests/native-sidecar-process.test.ts -t "layer"`, and root `pnpm check-types`.
---
## 2026-04-10 03:26:37 PDT - US-056
- Implemented multi-layer VM root composition in the Rust root filesystem bootstrap path by treating bootstrap directory entries as `mkdir -p` over merged lowers, which lets the writable upper override multi-layer roots without rejecting shared directories like `/workspace`.
- Added focused coverage in `crates/kernel/tests/root_fs.rs` and `crates/sidecar/tests/layer_management.rs` to verify highest-first lower precedence plus writable-upper overrides during direct root construction and sidecar VM creation.
- Recorded the reusable layering rule in `crates/kernel/AGENTS.md` (`crates/kernel/CLAUDE.md` target).
- Files changed: `crates/kernel/src/root_fs.rs`, `crates/kernel/tests/root_fs.rs`, `crates/sidecar/tests/layer_management.rs`, `crates/kernel/AGENTS.md`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Multi-layer VM roots should be expressed as immutable `lowers` ordered highest-to-lowest precedence plus `bootstrap_entries` for the single writable upper; tests should assert all three precedence levels explicitly.
  - Replaying upper snapshots over merged lowers must tolerate duplicate directory paths with `mkdir` semantics, or valid upper snapshots will fail early with `EEXIST` before file overrides are applied.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test root_fs -- --test-threads=1`, `cargo test -p agent-os-sidecar --test layer_management -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 03:30:59 PDT - US-057
- Expanded the dedicated package-level layer lifecycle suite so `packages/core/tests/layers.test.ts` now covers imported snapshot reuse plus multi-layer composition with a sealed upper snapshot, complementing the existing sidecar VM-isolation coverage.
- Added a reusable testing note under `packages/core/AGENTS.md` (`packages/core/CLAUDE.md` target) pointing future layer work at the package-level lifecycle suite and the Rust sidecar `layer_management` suite, then re-verified the real sidecar layer RPC smoke path.
- Files changed: `packages/core/CLAUDE.md`, `packages/core/tests/layers.test.ts`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Layer lifecycle regressions need both the in-memory package suite and the Rust sidecar suite: the former proves snapshot reuse/composition semantics, and the latter proves VM ownership scoping.
  - A sealed writable layer is the right reusable upper fixture for multi-layer tests because it exercises both the seal transition and later read-only reopening in one path.
  - The active truth path for this story is `pnpm --dir packages/core exec vitest run tests/layers.test.ts`, `pnpm --dir packages/core exec vitest run tests/native-sidecar-process.test.ts -t "layer RPCs"`, `cargo test -p agent-os-sidecar --test layer_management -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 03:39:36 PDT - US-058
- Implemented kernel-owned user/group identity plumbing by extending `UserConfig` / `UserManager` with group metadata, adding a reusable `ProcessIdentity` model, storing that identity on kernel process metadata, and exposing explicit kernel accessors for `getuid`, `getgid`, `geteuid`, `getegid`, `getgroups`, `getpwuid`, and `getgrgid`.
- Added focused coverage for the new kernel identity path, including process-metadata assertions, ownership checks for identity queries, and unit coverage for group normalization/rendering. Also recorded the reusable identity-state rule in `crates/kernel/CLAUDE.md` (`crates/kernel/AGENTS.md` target).
- Files changed: `crates/kernel/CLAUDE.md`, `crates/kernel/src/kernel.rs`, `crates/kernel/src/process_table.rs`, `crates/kernel/src/user.rs`, `crates/kernel/tests/identity.rs`, `crates/kernel/tests/user.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Kernel identity should be modeled once and copied into per-process metadata at registration time, so guest-facing uid/gid syscalls can read process-owned state without reverse-engineering env vars.
  - Group membership needs primary-gid normalization and deduplication up front; otherwise `getgroups()` and rendered `/etc/group`-style entries drift on duplicate primary gids.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test user -- --test-threads=1`, `cargo test -p agent-os-kernel --test identity -- --test-threads=1`, and root `pnpm check-types`.
---
## 2026-04-10 04:04 PDT - US-059
- Added guest-visible identity coverage for JavaScript, Python, and WASM, then wired the runtime env assembly so all three paths expose kernel-owned user, group, home, shell, and cwd defaults instead of runtime fallbacks.
- Fixed the host-node compatibility layers that those tests exercise: JS/V8 now keys implicit cwd off `PWD`, the embedded WASM runner now materializes virtual uid/gid and passwd data from env, and the Pyodide runner now receives the VM identity env during bootstrap.
- Files changed: `crates/kernel/src/kernel.rs`, `crates/sidecar/src/execution.rs`, `crates/sidecar/src/vm.rs`, `crates/sidecar/tests/guest_identity.rs`, `crates/execution/AGENTS.md`, `crates/execution/assets/runners/python-runner.mjs`, `crates/execution/assets/v8-bridge.js`, `crates/execution/assets/v8-bridge.source.js`, `crates/execution/src/javascript.rs`, `crates/execution/src/node_import_cache.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Guest-visible identity tests should exercise all three runtime front doors, because JS, WASM, and Python each have their own bootstrap layer that can leak a default like Pyodide's `web_user` or a stale host-node asset.
  - `prepare_guest_runtime_env(...)` is the shared truth path for VM identity defaults; if one runtime bypasses it, `HOME`/`PWD` split and kernel-owned `USER`/`SHELL` values drift immediately.
  - When changing bundled runner assets under `crates/execution/`, bump `NODE_IMPORT_CACHE_ASSET_VERSION` or stale `/tmp/agent-os-node-import-cache-*` materializations will mask the fix.
  - The active truth path for this story is `cargo test -p agent-os-sidecar --test guest_identity -- --test-threads=1` and root `pnpm check-types`.
---
## 2026-04-10 04:08:57 PDT - US-060
- Added root filesystem bootstrap suppression for kernel-reserved pseudo-filesystem paths so `bootstrap_entries` can no longer materialize writable-upper content under `/dev`, `/proc`, or `/sys`.
- Added focused `crates/kernel/tests/root_fs.rs` coverage proving reserved bootstrap paths are ignored while normal workspace entries still apply and snapshot cleanly, and recorded the reusable rule in `crates/kernel/AGENTS.md` (`crates/kernel/CLAUDE.md` target).
- Files changed: `crates/kernel/CLAUDE.md`, `crates/kernel/src/root_fs.rs`, `crates/kernel/tests/root_fs.rs`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- **Learnings for future iterations:**
  - Root filesystem bootstrap filtering belongs in `crates/kernel/src/root_fs.rs` so every VM construction path that replays `bootstrap_entries` inherits the same reserved-path suppression automatically.
  - Treat `/dev`, `/proc`, and `/sys` as kernel-owned pseudo-filesystem prefixes, not just single directories; writable-upper snapshots must not persist descendants under those trees.
  - The active truth path for this story is `cargo test -p agent-os-kernel --test root_fs -- --test-threads=1` and root `pnpm check-types`.
---
