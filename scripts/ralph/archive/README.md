Archived Ralph runs in this directory are historical snapshots only.

They may contain:
- stale `passes` fields
- superseded backlog priorities
- generic `pnpm test` or bare Vitest guidance that no longer matches the active repo state

Use `/home/nathan/a5/scripts/ralph/prd.json` and `/home/nathan/a5/scripts/ralph/progress.txt` as the current source of truth.
