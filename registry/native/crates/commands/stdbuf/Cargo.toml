[package]
name = "cmd-stdbuf"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "stdbuf standalone binary for Agent OS WasmVM"

[[bin]]
name = "stdbuf"
path = "src/main.rs"

[dependencies]
shims = { package = "secureexec-shims", path = "../../libs/shims" }
