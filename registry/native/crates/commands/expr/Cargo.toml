[package]
name = "cmd-expr"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "expr standalone binary for Agent OS WasmVM"

[[bin]]
name = "expr"
path = "src/main.rs"

[dependencies]
secureexec-expr = { path = "../../libs/expr" }
