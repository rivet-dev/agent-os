[package]
name = "cmd-mktemp"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "mktemp standalone binary for Agent OS WasmVM"

[[bin]]
name = "mktemp"
path = "src/main.rs"

[dependencies]
uu_mktemp = "0.7.0"
