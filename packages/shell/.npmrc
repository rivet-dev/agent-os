# Use npm-published software packages (not workspace links) because they
# include pre-built WASM binaries. The workspace copies have empty wasm/
# dirs since the native build (Rust nightly + wasi-sdk) is not run locally.
link-workspace-packages=false
