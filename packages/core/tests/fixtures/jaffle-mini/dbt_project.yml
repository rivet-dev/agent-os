name: 'jaffle'
version: '1.0.0'
config-version: 2
profile: 'jaffle'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  jaffle:
    staging:
      +materialized: view
    marts:
      +materialized: table
